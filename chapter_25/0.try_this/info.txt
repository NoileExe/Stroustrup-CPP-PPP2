25.3.1 - Выполните программу, приведенную выше, и выведите на печать адреса и размеры созданных объектов, чтобы увидеть, как возникают "дыры" в памяти и возникают ли они вообще. Если у вас есть время. можете нарисовать схему памяти, подобную показанным выше, чтобы лучше представить себе, как происходит фрагментация.

25.5.1 - Скомпилируйте приведенную выше программу и попробуйте поработать с ней, чтобы почувствовать двоичные и шестнадцатеричные представления чисел. Если вас удивляет представление отрицательных чисел, попробуйте еще раз после чтения раздела 25.5.3.

25.5.3 - Следующий пример может показаться безобидным, но он содержит бесконечный цикл:
void infinite()
{
	unsigned char mах = 160; // Очень большое
	for (signed char i=0; i < mах; ++i)
		cout << int(i) << '\n';
}

Выполните его и объясните, почему это происходит.

25.5.6 - Ключом было слово bs; что представляет собой текст? (ФАЙЛ "in.txt" лежит рядом с исходником и должен находится рядом с исполняемым файлом перед его запуском)